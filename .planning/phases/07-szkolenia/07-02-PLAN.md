---
phase: 07-szkolenia
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/lib/types.ts
  - src/lib/constants.ts
  - src/components/sections/SzkoleniaSection.tsx
autonomous: true

must_haves:
  truths:
    - "CTA Poznaj nasze szkolenia jest duzym tekstem-linkiem 44px w600 (nie malym border-button)"
    - "CTA ma hover na zielony (#9AFC4E / text-fm-green)"
    - "CTA tekst ma line break: Poznaj<br>nasze szkolenia"
    - "CTA linkuje do /szkolenia przez next/link Link"
    - "5 ikon technologii jest widocznych: SPIKE, Raspberry Pi, Arduino, Python, AI"
    - "Ikony sa w kontenerach 140px szerokosci z flex layout"
    - "SPIKE (pierwsza ikona) ma border-l border-[#FFFFFF54]"
    - "Ikony maja brightness-0 invert (bez opacity-60)"
    - "Strzalka-w-dol.png jest widoczna pod sekcja jako separator (60% width, opacity-40)"
    - "TECH_ICONS tablica jest zdefiniowana w constants.ts z 5 elementami"
    - "TechIcon interface jest zdefiniowany w types.ts"
    - "Brak etykiety uppercase Technologie nad ikonami"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "Interface TechIcon z src, alt, width"
      contains: "TechIcon"
    - path: "src/lib/constants.ts"
      provides: "Tablica TECH_ICONS z 5 ikonami technologii"
      contains: "TECH_ICONS"
    - path: "src/components/sections/SzkoleniaSection.tsx"
      provides: "Kompletna sekcja z CTA 44px, 5 ikonami technologii i strzalka separator"
      contains: "Poznaj"
  key_links:
    - from: "src/components/sections/SzkoleniaSection.tsx"
      to: "src/lib/constants.ts"
      via: "import TECH_ICONS"
      pattern: "import.*TECH_ICONS.*constants"
    - from: "src/components/sections/SzkoleniaSection.tsx"
      to: "src/lib/types.ts"
      via: "TechIcon uzywany w renderowaniu ikon"
      pattern: "TechIcon"
---

<objective>
Dodac CTA "Poznaj nasze szkolenia" jako duzy tekst-link 44px (nie border-button), 5 ikon technologii (SPIKE, Raspberry Pi, Arduino, Python, AI) w kontenerach 140px oraz strzalke separator do SzkoleniaSection.

Purpose: Kompletna sekcja Szkolenia — CTA i ikony technologii to kluczowe elementy wizualne sekcji. Bez nich sekcja jest niekompletna wzgledem Elementora.
Output: types.ts z TechIcon, constants.ts z TECH_ICONS, zaktualizowany SzkoleniaSection.tsx z CTA, ikonami i separatorem.
</objective>

<execution_context>
@C:/Users/mstrz/.claude-fundacja/get-shit-done/workflows/execute-plan.md
@C:/Users/mstrz/.claude-fundacja/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-szkolenia/07-RESEARCH.md
@.planning/phases/07-szkolenia/07-01-SUMMARY.md
@src/components/sections/SzkoleniaSection.tsx
@src/lib/constants.ts
@src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dodac TechIcon interface i TECH_ICONS tablica</name>
  <files>src/lib/types.ts, src/lib/constants.ts</files>
  <action>
**types.ts — dodac interface TechIcon:**

```typescript
export interface TechIcon {
  src: string;
  alt: string;
  width: number; // naturalna szerokosc w px z Elementora
}
```

Dodac na koncu pliku, po istniejacych interfejsach.

**constants.ts — dodac tablice TECH_ICONS:**

```typescript
import type { TechIcon } from "./types";

export const TECH_ICONS: TechIcon[] = [
  { src: "/images/23744_20138_spike5a.png", alt: "SPIKE", width: 149 },
  { src: "/images/raspberry-pi-svgrepo-com.png", alt: "Raspberry Pi", width: 34 },
  { src: "/images/arduino-logo-vector-01.png", alt: "Arduino", width: 63 },
  { src: "/images/python-svgrepo-com.png", alt: "Python", width: 45 },
  { src: "/images/ai.png", alt: "AI", width: 47 },
];
```

Dodac po istniejacych eksportach (np. po TRAINING_SECTION_NAV lub KNOWLEDGE_SECTION_NAV).

**UWAGA:** Sprawdzic czy `import type { ... } from "./types"` juz istnieje w constants.ts — jesli tak, dodac `TechIcon` do istniejacego importu. Jesli nie — dodac nowy import.
  </action>
  <verify>
`npx tsc --noEmit` — TypeScript kompiluje sie bez bledow. Grep: `TechIcon` w types.ts. Grep: `TECH_ICONS` w constants.ts. Grep: `arduino-logo-vector-01.png` w constants.ts (3. ikona). Grep: `python-svgrepo-com.png` w constants.ts (4. ikona). Grep: `ai.png` w constants.ts (5. ikona). Policz TECH_ICONS — powinno byc 5 elementow.
  </verify>
  <done>
types.ts zawiera interface TechIcon z src, alt, width. constants.ts eksportuje TECH_ICONS z 5 ikonami technologii: SPIKE (149px), Raspberry Pi (34px), Arduino (63px), Python (45px), AI (47px). Wszystkie pliki istnieja w public/images/. TypeScript kompiluje sie bez bledow.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dodac CTA tekst-link 44px, ikony technologii i strzalke separator</name>
  <files>src/components/sections/SzkoleniaSection.tsx</files>
  <action>
W SzkoleniaSection.tsx, w miejscu placeholder komentarza z Plan 07-01, dodac:

**1. Import Link z next/link** (na gorze pliku).
**2. Import TECH_ICONS z constants.ts** (dodac do istniejacego importu z constants).

**3. CTA — duzy tekst-link 44px (ZAMIAST border-button):**

```tsx
{/* CTA — duzy tekst-link 44px, w600 */}
<div className="mb-16">
  <Link
    href="/szkolenia"
    className="inline-block text-[44px] font-semibold leading-none text-white transition-colors hover:text-fm-green"
    style={{
      fontFamily: "'neue-haas-grotesk-display', var(--font-sans)",
    }}
  >
    Poznaj
    <br />
    nasze szkolenia
  </Link>
</div>
```

**KLUCZOWE ROZNICE vs obecny kod:**
- Font-size: 44px (nie text-sm)
- Font-weight: semibold/w600 (nie default)
- Brak bordera, brak paddingu, brak uppercase — to jest czysty tekst-link
- Hover: text-fm-green (#9AFC4E) — nie hover:border-fm-green
- Tekst z line break: "Poznaj\nnasze szkolenia"
- Font family: neue-haas-grotesk-display

**4. Rzad ikon technologii (5 ikon w kontenerach 140px):**

```tsx
{/* Rzad ikon technologii */}
<div className="mb-16 flex flex-wrap items-center">
  {TECH_ICONS.map((icon, index) => (
    <div
      key={icon.alt}
      className={`flex h-16 items-center justify-center${
        index === 0 ? " border-l border-[#FFFFFF54]" : ""
      }`}
      style={{ width: 140 }}
    >
      <Image
        src={icon.src}
        alt={icon.alt}
        width={icon.width}
        height={40}
        className="h-8 w-auto brightness-0 invert"
      />
    </div>
  ))}
</div>
```

**KLUCZOWE:**
- 5 ikon (nie 2) — SPIKE, Raspberry Pi, Arduino, Python, AI
- Kontenery 140px szerokie (jak w Elementorze desktop)
- SPIKE (index === 0) ma border-l border-[#FFFFFF54] — separator od tresci powyzej
- brightness-0 invert — zachowac (ikony moga byc ciemne/kolorowe)
- BEZ opacity-60 — Elementor nie ma opacity na ikonach (tylko na overlayach)
- BEZ etykiety "Technologie" uppercase — nie istnieje w Elementorze

**5. Strzalka separator na koncu:**

```tsx
{/* Separator strzalka */}
<div className="mt-16 flex justify-center" style={{ width: "60%" }}>
  <Image
    src="/images/Strzalka-w-dol.png"
    alt=""
    width={59}
    height={13}
    className="opacity-40"
  />
</div>
```

Identyczny wzorzec jak Phase 4-6.
  </action>
  <verify>
`npx next build` — build przechodzi bez bledow. Grep: `text-\[44px\]` w SzkoleniaSection.tsx (CTA 44px). Grep: `font-semibold` w SzkoleniaSection.tsx (CTA w600). Grep: `hover:text-fm-green` w SzkoleniaSection.tsx (hover zielony). Grep: `TECH_ICONS.map` w SzkoleniaSection.tsx (renderowanie ikon). Grep: `border-l border-\[#FFFFFF54\]` w SzkoleniaSection.tsx (SPIKE separator). Grep: `Strzalka-w-dol.png` w SzkoleniaSection.tsx (separator). Sprawdzic ze NIE ma: `border border-white`, `text-sm`, `tracking-widest`, `uppercase`, `Technologie`, `opacity-60` w SzkoleniaSection.tsx.
  </verify>
  <done>
CTA "Poznaj nasze szkolenia" jako duzy tekst-link 44px w600 z hover na zielony (bez bordera, bez tla). 5 ikon technologii (SPIKE, Raspberry Pi, Arduino, Python, AI) w kontenerach 140px z brightness-0 invert (bez opacity-60). SPIKE ma border-l separator. Strzalka separator na koncu sekcji. Brak etykiety "Technologie", brak border-button. Build przechodzi.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript kompiluje sie bez bledow
2. `npx next build` — build przechodzi
3. Grep: `TechIcon` w types.ts — interface istnieje
4. Grep: `TECH_ICONS` w constants.ts — tablica z 5 elementami
5. Grep: `text-\[44px\]` w SzkoleniaSection.tsx — CTA 44px (nie text-sm)
6. Grep: `font-semibold` w SzkoleniaSection.tsx — CTA w600
7. Grep: `TECH_ICONS.map` w SzkoleniaSection.tsx — 5 ikon
8. Grep: `Strzalka-w-dol.png` w SzkoleniaSection.tsx — separator
9. Grep: `border-l border-\[#FFFFFF54\]` w SzkoleniaSection.tsx — SPIKE border
10. Brak w SzkoleniaSection.tsx: `border border-white`, `text-sm`, `tracking-widest`, `Technologie`, `opacity-60`
</verification>

<success_criteria>
- CTA jest duzym tekstem-linkiem 44px w600 (nie border-button)
- CTA ma hover na zielony, tekst z <br> ("Poznaj\nnasze szkolenia")
- 5 ikon technologii w kontenerach 140px z brightness-0 invert
- SPIKE ma border-l separator #FFFFFF54
- Strzalka separator pod sekcja
- TECH_ICONS w constants.ts, TechIcon w types.ts
- Brak elementow nieistniejacych w Elementorze (etykieta Technologie, opacity-60)
- `next build` przechodzi bez bledow
</success_criteria>

<output>
After completion, create `.planning/phases/07-szkolenia/07-02-SUMMARY.md`
</output>
