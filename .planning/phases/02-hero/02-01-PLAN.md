---
phase: 02-hero
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/HeroSection.tsx
  - src/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Sekcja Hero zajmuje dokladnie 100vh (h-screen, nie min-h-screen)"
    - "Logo FMF-white.png widoczne w lewym gornym rogu z marginesem"
    - "Ilustracja dzieci-panorama4.png wyswietla sie jako dekoracyjne tlo sekcji (element DOM, nie background-image)"
    - "Tekst misji (4 paragrafy) czytelny na tle ilustracji, bez zielonych akcentow"
    - "Strzalka w dol (Strzalka-w-dol.png) klikalna — scrolluje smooth do #programy-edukacyjne"
    - "HeroSection wyekstrahowany do osobnego pliku w components/sections/"
  artifacts:
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Pelny komponent HeroSection z logo, ilustracja, tekstem misji, scroll indicator"
      exports: ["HeroSection"]
    - path: "src/app/page.tsx"
      provides: "Import HeroSection z components/sections/ zamiast lokalnej funkcji"
      contains: "import.*HeroSection.*from.*components/sections/HeroSection"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/sections/HeroSection.tsx"
      via: "import { HeroSection }"
      pattern: "import.*HeroSection.*from.*@/components/sections/HeroSection"
    - from: "src/components/sections/HeroSection.tsx"
      to: "/images/dzieci-panorama4.png"
      via: "next/image z fill prop"
      pattern: "dzieci-panorama4\\.png"
    - from: "src/components/sections/HeroSection.tsx"
      to: "#programy-edukacyjne"
      via: "anchor link na scroll indicator"
      pattern: "href.*#programy-edukacyjne"
---

<objective>
Wyekstrahowac HeroSection z page.tsx do osobnego komponentu i przebudowac: zamienic inline SVG rakiete na prawdziwa ilustracje (dzieci-panorama4.png), ustawic dokladnie 100vh, zrobic strzalke klikalna.

Purpose: Sekcja Hero jest pierwsza rzecza ktora widzi uzytkownik — musi wiernie odwzorowac oryginal futureminds.edu.pl z prawdziwa ilustracja zamiast placeholder SVG.
Output: Dzialajacy HeroSection.tsx w components/sections/ z poprawnym importem w page.tsx.
</objective>

<execution_context>
@C:\Users\mstrz\.claude-fundacja/get-shit-done/workflows/execute-plan.md
@C:\Users\mstrz\.claude-fundacja/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero/02-RESEARCH.md
@src/app/page.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utworz HeroSection.tsx z prawdziwa ilustracja i klikalnym scroll indicator</name>
  <files>src/components/sections/HeroSection.tsx</files>
  <action>
Utworz plik src/components/sections/HeroSection.tsx. Bazuj na istniejacym kodzie HeroSection z page.tsx (linie 17-181) ale z nastepujacymi zmianami:

1. **Eksport komponentu:** `export function HeroSection()` (named export)

2. **Section:** Zamienic `min-h-screen` na `h-screen`. Zachowac `relative`, `flex flex-col justify-center`. NIE uzywac `overflow: hidden` — uzywac `overflow-clip` jezeli w ogole potrzebne (clip nie tworzy scroll context). Tlo: `bg-fm-dark`.

3. **Ilustracja rakiety (ZASTAPIC caly inline SVG):**
   - Usunac caly blok `<div className="pointer-events-none absolute inset-0"><svg ...></svg></div>` (~100 linii inline SVG)
   - Zastapic elementem DOM z next/image:
   ```tsx
   <div className="pointer-events-none absolute inset-0">
     <Image
       src="/images/dzieci-panorama4.png"
       alt=""
       fill
       className="object-contain object-bottom opacity-[0.15]"
       aria-hidden="true"
     />
   </div>
   ```
   - `fill` prop wymaga rodzica z `position: relative` — section juz ma `relative`
   - `object-contain object-bottom` — ilustracja na dole ekranu jak na oryginale
   - `opacity-[0.15]` — start, subtelna ale widoczna (dopasowac wizualnie pozniej)
   - `priority={false}` (domyslne) — NIE ladowac z priorytetem (906KB dekoracja)
   - `aria-hidden="true"` — element dekoracyjny, niewidoczny dla screenreaderow

4. **Logo:** Zachowac bez zmian — `priority={true}`, width/height, mb-20, h-16, w-auto

5. **Tekst misji:** Zachowac DOKLADNIE istniejace 4 paragrafy z polskimi znakami diakrytycznymi. BEZ zielonych akcentow (na oryginale tekst Hero jest jednokolorowy fm-text/#c4c4c4). Klasy: `text-lg leading-relaxed font-light text-fm-text` (zachowac z obecnego kodu).

6. **Scroll indicator (ZASTAPIC div na a):**
   - Zamienic `<div className="absolute bottom-12 ...">` na:
   ```tsx
   <a
     href="#programy-edukacyjne"
     className="absolute bottom-8 left-1/2 z-10 -translate-x-1/2"
     aria-label="Przewin w dol"
   >
     <Image
       src="/images/Strzalka-w-dol.png"
       alt=""
       width={59}
       height={13}
       className="animate-bounce opacity-60 transition-opacity hover:opacity-100"
     />
   </a>
   ```
   - Dodac `z-10` na scroll indicator aby byl klikalny nad ilustracja
   - `aria-label` zamiast alt na strzalce (bo a ma label)
   - hover:opacity-100 — feedback na hover

7. **Z-index na warstwie tresci:** Dodac `z-10` do div-a z logo i tekstem misji aby tresc byla nad ilustracja.

8. **Czego NIE robic:**
   - NIE dodawac zielonych akcentow (#9AFC4E) w tekscie misji
   - NIE uzywac background-image CSS
   - NIE uzywac overflow: hidden (moze psuc sticky w sekcjach ponizej)
   - NIE zmieniac tekstu misji (jest juz poprawny)
   - NIE ustawiac priority={true} na ilustracji rakiety
  </action>
  <verify>
Plik istnieje: `ls src/components/sections/HeroSection.tsx`
Eksportuje HeroSection: grep "export function HeroSection" src/components/sections/HeroSection.tsx
Uzywa dzieci-panorama4.png: grep "dzieci-panorama4" src/components/sections/HeroSection.tsx
Ma h-screen (nie min-h-screen): grep "h-screen" src/components/sections/HeroSection.tsx && ! grep "min-h-screen" src/components/sections/HeroSection.tsx
Strzalka jest linkiem: grep "href.*#programy-edukacyjne" src/components/sections/HeroSection.tsx
Brak inline SVG: ! grep "viewBox" src/components/sections/HeroSection.tsx
  </verify>
  <done>HeroSection.tsx eksportuje komponent z: h-screen, dzieci-panorama4.png jako Image z fill, 4 paragrafy tekstu bez zielonych akcentow, klikalna strzalka scrollujaca do #programy-edukacyjne</done>
</task>

<task type="auto">
  <name>Task 2: Zaktualizuj page.tsx — import HeroSection i usun lokalna funkcje</name>
  <files>src/app/page.tsx</files>
  <action>
W pliku src/app/page.tsx:

1. **Dodaj import:** Na gorze pliku dodac:
   ```tsx
   import { HeroSection } from "@/components/sections/HeroSection";
   ```

2. **Usun lokalna funkcje HeroSection:** Usunac cala funkcje `function HeroSection() { ... }` (linie ~17-181 — od komentarza `/* HERO */` do konca zamykajacego `}` przed nastepnym komentarzem sekcji).

3. **Zachowaj uzycie w Home():** `<HeroSection />` w JSX komponentu Home() zostaje bez zmian — teraz importowany zamiast lokalny.

4. **Sprawdz importy:** Upewnij sie ze Image z "next/image" jest nadal importowany (uzywany w innych sekcjach page.tsx). Link z "next/link" tez zostaje.

5. **Czego NIE robic:**
   - NIE zmieniac zadnej innej sekcji (ProgramyEdukacyjne, Stypendialne, Projekty itd.)
   - NIE zmieniac komponentu Home() poza usunieciem lokalnej funkcji HeroSection
   - NIE zmieniac importow constants ani StickySection
  </action>
  <verify>
Build przechodzi: `npx next build` (lub przynajmniej: `npx tsc --noEmit`)
Import istnieje: grep "import.*HeroSection.*from.*@/components/sections/HeroSection" src/app/page.tsx
Lokalna funkcja usunieta: ! grep "function HeroSection" src/app/page.tsx
HeroSection uzywany w JSX: grep "<HeroSection" src/app/page.tsx
  </verify>
  <done>page.tsx importuje HeroSection z components/sections/, lokalna funkcja HeroSection usunieta, `npx tsc --noEmit` przechodzi bez bledow</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — brak bledow TypeScript
2. `npx next build` — build przechodzi
3. Wizualnie: `npx next dev` -> localhost:3000 -> sekcja Hero zajmuje pelny ekran, widoczna ilustracja rakiety, logo w lewym gornym rogu, 4 paragrafy tekstu, klikalna strzalka na dole
4. Klikniecie strzalki scrolluje smooth do sekcji Programy Edukacyjne
</verification>

<success_criteria>
- HeroSection.tsx istnieje w src/components/sections/ i eksportuje komponent
- page.tsx importuje HeroSection zamiast definiowac lokalna funkcje
- Hero zajmuje dokladnie 100vh (h-screen)
- Ilustracja dzieci-panorama4.png wyswietla sie jako dekoracyjne tlo (Image z fill)
- Strzalka w dol jest klikalnym anchor linkiem do #programy-edukacyjne
- Build przechodzi bez bledow
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero/02-01-SUMMARY.md`
</output>
