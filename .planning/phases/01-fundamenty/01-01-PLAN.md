---
phase: 01-fundamenty
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Strona uzywa koloru #9AFC4E (lime green) zamiast starego #00d084"
    - "Font Plus Jakarta Sans laduje sie na stronie (nie Inter, nie Cardo)"
    - "Hierarchia typografii jest zdefiniowana w @theme — 5 poziomow (section, heading, sidebar, body, cta)"
    - "Naglowki sekcji (text-section) uzywaja font-weight 200 (extra-light, ultra-cienkie)"
    - "Font jest zaimplementowany jako CSS variable — latwa podmiana na inny"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Paleta kolorow FM + hierarchia typografii w @theme + font w @theme inline"
      contains: "--color-fm-green: #9AFC4E"
    - path: "src/app/layout.tsx"
      provides: "Plus Jakarta Sans via next/font/google, bez Inter, bez Cardo"
      contains: "Plus_Jakarta_Sans"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS variable --font-plus-jakarta podpieta w @theme inline jako --font-sans"
      pattern: "@theme inline.*--font-sans.*var\\(--font-plus-jakarta\\)"
---

<objective>
Zaktualizowac palete kolorow, font i hierarchie typografii — bazowe tokeny wizualne strony.

Purpose: Wszystkie komponenty uzywaja tokenow z globals.css (@theme). Zmiana koloru zielonego z #00d084 na #9AFC4E, zamiana fontu Inter na Plus Jakarta Sans, i zdefiniowanie 5 poziomow typografii to fundament dla calej strony.
Output: Zaktualizowane globals.css i layout.tsx — strona renderuje sie z prawidlowym fontem, kolorem i typografia.
</objective>

<execution_context>
@C:\Users\mstrz\.claude-fundacja/get-shit-done/workflows/execute-plan.md
@C:\Users\mstrz\.claude-fundacja/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundamenty/01-CONTEXT.md
@.planning/phases/01-fundamenty/01-RESEARCH.md
@src/app/globals.css
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aktualizacja globals.css — paleta kolorow + typografia + font</name>
  <files>src/app/globals.css</files>
  <action>
Zastap cala zawartosc globals.css nowa wersja:

1. **@theme inline** (MUSI byc przed @theme) — podpiecie fontu:
   - `--font-sans: var(--font-plus-jakarta);` — referuje CSS variable z next/font
   - UWAGA: uzywac `@theme inline` (NIE `@theme`) bo wartosc referuje inna CSS variable. Bez `inline` font wrocil do fallback.

2. **@theme** — literalne wartosci:
   - Paleta kolorow (KRYTYCZNE: `--color-fm-green: #9AFC4E` nie #00d084):
     - `--color-fm-green: #9AFC4E` (lime green — prawdziwy kolor z Elementor)
     - `--color-fm-dark: #32373c` (ciemne tlo)
     - `--color-fm-dark-lighter: #40454a`
     - `--color-fm-dark-bg: #2b2f33`
     - `--color-fm-white: #ffffff`
     - `--color-fm-text: #c4c4c4`
     - `--color-fm-text-muted: #8a8a8a`
     - `--color-fm-gray-100: #f0f2f4`
     - `--color-fm-gray-200: #e2e5e9`
     - `--color-fm-gray-400: #9ca3af`
     - `--color-fm-gray-600: #6b7280`
   - Usunac: `--color-fm-green-dark: #00b371` i `--color-fm-turquoise: #8ed1fc` (nie uzywane w oryginale)
   - Usunac: `--font-serif` (Cardo nie jest uzywany na stronie)
   - Hierarchia typografii — 5 poziomow z line-height, font-weight i opcjonalnie letter-spacing:
     - section: 100px / line-height 1.1 / weight 200 (extra-light — najblizsze do oryginalu thin, Plus Jakarta Sans nie ma wagi 100)
     - heading: 42px / line-height 1.2 / weight 600
     - sidebar: 30px / line-height 1.3 / weight 600
     - body: 19px / line-height 1.7 / weight 200
     - cta: 10px / line-height 1.5 / weight 400 / letter-spacing 0.15em
   - Format nazw: `--text-section`, `--text-heading`, `--text-sidebar`, `--text-body`, `--text-cta`
   - Postfiksy Tailwind v4: `--text-X--line-height`, `--text-X--font-weight`, `--text-X--letter-spacing`

3. **Selektory globalne** (zachowac):
   - `html { scroll-behavior: smooth; }`
   - `body { font-family: var(--font-sans); color: var(--color-fm-text); background-color: var(--color-fm-dark); }`

Dokladny wzorzec kodu jest w 01-RESEARCH.md sekcja "Kompletny globals.css po aktualizacji". Uzyc go jako bazy.
  </action>
  <verify>
Sprawdzic ze plik zawiera: `#9AFC4E` (nie `#00d084`), `@theme inline`, `--font-sans: var(--font-plus-jakarta)`, `--text-section`, `--text-heading`, `--text-sidebar`, `--text-body`, `--text-cta`. Sprawdzic ze NIE zawiera: `#00d084`, `Inter`, `Cardo`, `--font-serif`, `--color-fm-turquoise`.
  </verify>
  <done>globals.css zawiera prawidlowa palete (#9AFC4E), 5 poziomow typografii w @theme, font podpiety przez @theme inline</done>
</task>

<task type="auto">
  <name>Task 2: Aktualizacja layout.tsx — zamiana Inter/Cardo na Plus Jakarta Sans</name>
  <files>src/app/layout.tsx</files>
  <action>
Zastap importy fontow i konfiguracje w layout.tsx:

1. **Usunac**: `import { Inter, Cardo } from "next/font/google"` — oba fonty nie sa uzywane na oryginalnej stronie
2. **Dodac**: `import { Plus_Jakarta_Sans } from "next/font/google"`
3. **Skonfigurowac** Plus Jakarta Sans jako variable font:
   ```tsx
   const plusJakartaSans = Plus_Jakarta_Sans({
     subsets: ["latin", "latin-ext"],
     variable: "--font-plus-jakarta",
     display: "swap",
     // NIE podawac weight — variable font automatycznie laduje wagi 200-800
   });
   ```
   UWAGA: variable MUSI byc `--font-plus-jakarta` — to ta sama nazwa co w globals.css `@theme inline { --font-sans: var(--font-plus-jakarta); }`.
4. **Zaktualizowac** className na html element: `className={plusJakartaSans.variable}` (jeden font, nie dwa)
5. **Usunac**: stale `inter` i `cardo` — nie sa juz potrzebne
6. **Zachowac** reszty pliku bez zmian (metadata, body, main, Footer)

Dokladny wzorzec kodu jest w 01-RESEARCH.md sekcja "Kompletny layout.tsx po aktualizacji".
  </action>
  <verify>
Uruchomic `npx next build` (lub `npm run build`) — build MUSI przejsc bez bledow. Sprawdzic ze layout.tsx: importuje `Plus_Jakarta_Sans` (nie `Inter`, nie `Cardo`), ustawia `variable: "--font-plus-jakarta"`, nie zawiera `inter.variable` ani `cardo.variable`.
  </verify>
  <done>layout.tsx laduje Plus Jakarta Sans jako jedyny font, Inter i Cardo usuniete, build przechodzi bez bledow</done>
</task>

</tasks>

<verification>
1. `npm run build` — brak bledow TypeScript i budowania
2. Uruchomic `npm run dev`, otworzyc strone w przegladarce
3. DevTools > Computed Styles na body: font-family powinno zawierac "Plus Jakarta Sans" (nie "Inter")
4. DevTools > Computed Styles na dowolnym elemencie z `text-fm-green`: kolor powinien byc #9AFC4E (nie #00d084)
5. Network tab: powinien ladowac sie font Plus Jakarta Sans (nie Inter, nie Cardo)
</verification>

<success_criteria>
- Kolor zielony to #9AFC4E na calej stronie
- Font to Plus Jakarta Sans (nie Inter)
- 5 poziomow typografii dostepne jako utility classes Tailwind (text-section, text-heading, text-sidebar, text-body, text-cta)
- Build przechodzi bez bledow
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundamenty/01-01-SUMMARY.md`
</output>
