---
phase: 01-fundamenty
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/constants.ts
autonomous: true

must_haves:
  truths:
    - "constants.ts zawiera 9 partnerow (nie 5) — Rockwell, John Deere, RTX, Collins Aerospace, ENEA + 4 dodatkowych"
    - "Kazdy partner, program, projekt ma prawidlowy interfejs TypeScript"
    - "constants.ts zawiera 6 projektow z opisami"
    - "constants.ts zawiera 3 programy edukacyjne (FLL, BtC, OZEdukacja)"
    - "Struktura danych jest gotowa na przyszla zamiane na fetch z Supabase (interfejsy osobno)"
    - "Dane artykulow/publikacji sa zdefiniowane do uzycia przez sekcje Baza Wiedzy"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "Interfejsy TypeScript: Partner, Program, Project, Article, NavItem, StickyNavLink"
      exports: ["Partner", "Program", "Project", "Article", "NavItem"]
    - path: "src/lib/constants.ts"
      provides: "Pelne dane statyczne: 9 partnerow, 3 programy, 6 projektow, artykuly, nawigacja"
      contains: "PARTNER_LOGOS"
  key_links:
    - from: "src/lib/constants.ts"
      to: "src/lib/types.ts"
      via: "import typow (Partner, Program, Project, Article)"
      pattern: "import.*from.*types"
    - from: "src/app/page.tsx"
      to: "src/lib/constants.ts"
      via: "import danych do komponentow sekcji"
      pattern: "import.*from.*constants"
---

<objective>
Zaktualizowac types.ts i constants.ts z pelnym zestawem danych i interfejsow TypeScript.

Purpose: Wszystkie sekcje strony (fazy 2-9) uzywaja danych z constants.ts. Aktualizacja do pelnych 9 partnerow, 6 projektow, artykulow i czyste interfejsy TS to warunek dla dalszych faz.
Output: Zaktualizowane types.ts z interfejsami + constants.ts z pelnymi danymi.
</objective>

<execution_context>
@C:\Users\mstrz\.claude-fundacja/get-shit-done/workflows/execute-plan.md
@C:\Users\mstrz\.claude-fundacja/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundamenty/01-CONTEXT.md
@.planning/phases/01-fundamenty/01-RESEARCH.md
@src/lib/types.ts
@src/lib/constants.ts
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rozszerzenie types.ts o pelne interfejsy</name>
  <files>src/lib/types.ts</files>
  <action>
Zaktualizowac types.ts — rozszerzyc istniejace interfejsy i dodac brakujace:

1. **Partner** — rozszerzyc o opcjonalne pole `href`:
   ```ts
   export interface Partner {
     name: string;
     src: string;
     href?: string;
   }
   ```

2. **Program** — bez zmian, juz kompletny:
   ```ts
   export interface Program {
     title: string;
     description: string;
     href: string;
     logo: string;
   }
   ```

3. **Project** — dodac opcjonalne `href`:
   ```ts
   export interface Project {
     title: string;
     description: string;
     image?: string;
     href?: string;
   }
   ```

4. **Article** — NOWY interfejs (potrzebny dla sekcji Baza Wiedzy):
   ```ts
   export interface Article {
     title: string;
     author?: string;
     category: string;
     href: string;
     image?: string;
   }
   ```

5. **NavItem** — bez zmian, juz kompletny.

6. **StickyNavLink** — NOWY interfejs (przeniesiony z StickySection.tsx, wspoldzielony):
   ```ts
   export interface StickyNavLink {
     label: string;
     href: string;
   }
   ```

Wszystkie interfejsy eksportowane — gotowe na przyszla zamiane constants na fetch z Supabase.
  </action>
  <verify>Sprawdzic ze types.ts eksportuje: Partner (z href?), Program, Project (z href?), Article (nowy), NavItem, StickyNavLink (nowy).</verify>
  <done>types.ts zawiera 6 interfejsow — Partner, Program, Project, Article, NavItem, StickyNavLink — wszystkie eksportowane</done>
</task>

<task type="auto">
  <name>Task 2: Aktualizacja constants.ts z pelnym danymi</name>
  <files>src/lib/constants.ts</files>
  <action>
Zaktualizowac constants.ts — importowac typy z types.ts, rozszerzyc dane do pelnych:

1. **Import typow** z types.ts:
   ```ts
   import type { Partner, Program, Project, Article, NavItem, StickyNavLink } from "./types";
   ```

2. **PARTNER_LOGOS: Partner[]** — rozszerzyc z 5 do 9 partnerow. Istniejace 5:
   - Rockwell Automation (`/images/Logo-Rockwell.png`)
   - John Deere (`/images/jo.png`)
   - RTX (`/images/Logo-RTX.png`)
   - Collins Aerospace (`/images/Collins-aerospace-white.png`)
   - ENEA (`/images/logo-ENEA-3-biale.png`)

   Dodac 4 brakujacych na podstawie dostepnych obrazkow w public/images/:
   - Mmaltic (`/images/mmaltic.png`)
   - Collins Aerospace (wariant stack: `/images/Collins_Aerospace_logo_stack_white_300.png`) — UWAGA: moze to byc duplikat Collins-aerospace-white.png. Jesli tak, uzyc jako wariant lub pominac i zidentyfikowac inne logo partnera.
   - Zbadaj obrazki: `Screen-Shot-2019-12-27-at-1.50.07-PM.png` (i wersja 1024x232) — moga to byc dodatkowe loga partnerow.
   - Jesli nie da sie zidentyfikowac 9 unikalnych partnerow z dostepnych obrazkow, dodaj placeholder wpisy z komentarzem `// TODO: zidentyfikowac logo partnera` i nazwa partnera (bez src).

3. **PROGRAMS: Program[]** — zachowac 3 istniejace programy (FLL, Build the Change, OZEdukacja) bez zmian. Dane sa kompletne i prawidlowe.

4. **PROJECTS: Project[]** — zachowac 6 istniejacych projektow. Dodac brakujace obrazki do tych ktore ich nie maja:
   - "Realizacja grantu od IOM" — dodac `image: "/images/IOM.png"`
   - "Noc Naukowcow" — dodac `image: "/images/Noc-naukowcow.png"`
   - "Mistrzostwa IT" — brak oczywistego obrazka, moze `image: "/images/Untitled-designapt.png"` lub zostawic undefined
   - "Laboratoria Przyszlosci" — dodac `image: "/images/logo-Laboratoria_Przyszlosci_poziom_biale-1-e1707145414838.png"`

5. **ARTICLES: Article[]** — NOWA stala. Dodac 6 artykulow/publikacji (statyczne dane placeholder):
   - "Uczenie (sie) przez tworzenie" — Jakub Piasecki, kategoria "Baza wiedzy"
   - "Robotyka w programie Laboratoria Przyszlosci" — Wojciech Zuziak, kategoria "Baza wiedzy"
   - 4 dodatkowe placeholder artykuly z kategoria "Baza wiedzy" i sensownymi tytulami edukacyjnymi (np. o STEAM, robotyce, FLL). Tresci traktowane jako tymczasowe — placeholder do wymiany.

6. **Zachowac istniejace stale** bez zmian: NAV_ITEMS, EDUCATION_SECTION_NAV, SCHOLARSHIP_SECTION_NAV, PROJECTS_SECTION_NAV, TRAINING_SECTION_NAV, KNOWLEDGE_SECTION_NAV, FOOTER_LINKS, SOCIAL_LINKS. Dodac do nich adnotacje typow (np. `: NavItem[]`, `: StickyNavLink[]`).

7. **Adnotacje typow** na WSZYSTKICH stalych — uzywac importowanych typow zamiast implicit.

WAZNE: Nie zmieniac nazw exportow (PARTNER_LOGOS, PROGRAMS, PROJECTS itp.) — page.tsx i inne komponenty juz je importuja.
  </action>
  <verify>
Uruchomic `npx tsc --noEmit` — brak bledow TypeScript. Sprawdzic: PARTNER_LOGOS ma >= 9 elementow (lub tyle ile zidentyfikowano + TODO komentarze), PROJECTS ma 6 elementow, ARTICLES istnieje z >= 6 elementami, wszystkie stale maja adnotacje typow.
  </verify>
  <done>constants.ts importuje typy z types.ts, zawiera 9 partnerow (lub max zidentyfikowanych), 6 projektow z obrazkami, 6 artykulow, wszystkie stale typowane</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — brak bledow TypeScript
2. `npm run build` — build przechodzi bez bledow
3. Sprawdzic ze page.tsx nadal poprawnie importuje PROGRAMS, PROJECTS, PARTNER_LOGOS z constants.ts (nazwy eksportow nie zmienione)
4. Policzyc elementy: PARTNER_LOGOS >= 9, PROJECTS === 6, ARTICLES >= 6
</verification>

<success_criteria>
- types.ts eksportuje 6 interfejsow (Partner, Program, Project, Article, NavItem, StickyNavLink)
- constants.ts importuje typy z types.ts i uzywa ich na wszystkich stalych
- PARTNER_LOGOS ma 9 partnerow (lub max zidentyfikowanych z obrazkow)
- PROJECTS ma 6 projektow z obrazkami gdzie dostepne
- ARTICLES ma 6 pozycji
- Build i TypeScript check przechodzi bez bledow
- Istniejace importy w page.tsx nie sa zepsute
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundamenty/01-02-SUMMARY.md`
</output>
